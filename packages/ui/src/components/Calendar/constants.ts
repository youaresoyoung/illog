export const DEFAULT_HOUR_HEIGHT = 60
export const DEFAULT_GUTTER_WIDTH = 64
export const DEFAULT_SCROLL_HEIGHT = 'calc(100vh - 160px)'

export const HOURS = Array.from({ length: 24 }, (_, i) => i) // 0-23
