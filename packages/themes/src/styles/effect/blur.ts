import { blur as blurSize } from '../../tokens/size/blur'

export const blur = {
  overlay: {
    blur: blurSize[100]
  },
  layer: {
    blur: blurSize[100]
  },
  glass: {
    blur: blurSize[100]
  }
}
