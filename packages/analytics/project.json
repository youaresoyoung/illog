{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "illog-analytics",
  "projectType": "library",
  "root": "packages/analytics",
  "sourceRoot": "packages/analytics/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "packages/analytics", "command": "pnpm build" },
      "outputs": ["{projectRoot}/dist"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "cwd": "packages/analytics", "command": "pnpm exec eslint --cache ." }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "cwd": "packages/analytics", "command": "pnpm exec tsc --noEmit" }
    }
  }
}
